import { AccountPage } from "@/components/pages/Account";
import { Metadata } from "next";


export const metadata: Metadata = {
    title: "Minha connta"
};

const Accounnt = () => <AccountPage />

export default Accounnt;